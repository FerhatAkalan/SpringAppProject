spring:
  output:
    ansi:
      enabled: always
  #datasource:
  #  url: jdbc:h2:./devdb
  #  username: sa
  jpa:
    #hibernate:
    #ddl-auto: update
    properties:
      javax:
        persistence:
          validation:
            mode: none
  main:
    allow-bean-definition-overriding: true
    allow-circular-references: true
